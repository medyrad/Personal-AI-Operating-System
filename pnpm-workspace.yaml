packages:
  - "apps/frontend"

allowBuilds:
  esbuild: true

# esbuild (a Vite dependency) needs its postinstall script to fetch the correct
# platform binary — explicitly allow-listed rather than allowing all build scripts.
onlyBuiltDependencies:
  - esbuild
